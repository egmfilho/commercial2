md-content.login(flex)
	.cover
	.picture


	div(layout='column' layout-align='center center' layout-fill)
		.logo
		md-card(style='width: 300px')
			md-card-content
				form(name='LoginForm' layout='column' ng-submit='login.submitForm()')
					md-input-container
						label Nome de Usuário
						input(type='text' name='user' ng-model='login.user' keys-shortcuts enter='login.advance()' required)
						div(ng-messages='LoginForm.user.$error' role='alert' multiple)
							div(ng-message='required') Informe o nome de usuário.
					
					md-input-container
						label Senha
						input(type='password' name='pass' ng-model='login.pass' md-maxlength='16' required)
						div(ng-messages='LoginForm.pass.$error' role='alert' multiple)
							div(ng-message='required') Informe a senha.
							div(ng-message='md-maxlength') Senha muito longa.

					.text-center.text-primary.offset-top-30(ng-if='login.loggingIn')
						i.fa.fa-3x.fa-spin.fa-circle-o-notch

					div(layout='column' ng-if='!login.loggingIn')
						//- md-button.md-raised.md-primary.offset-top-30(type='button' ng-click='login.selectApi()')
							| &nbsp; SELECIONAR API &nbsp;
							
						md-button.md-raised.md-primary.offset-top-15(ng-disabled='!LoginForm.$valid' type='submit')
							| &nbsp; Login &nbsp;

						.api
							p.text-center.text-light.offset-top-15 API: {{login.getCurrentApi().name}}
								br
								a.text-center(href='' ng-click='login.selectApi()') (Trocar)

		.lost-pass
			a(href='' ng-click='login.forgottenPassword()') Esqueci a senha

	.ver 
		span Versão {{version}}

	.sign 
		a(href='') Futura Agência - 2017